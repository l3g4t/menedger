"""
Тесты для vault.storage.

ЗАГЛУШКА. Будут реализованы вместе с vault/storage.py.

Планируемые тест-кейсы:
    - save_vault_file создаёт файл с ожидаемым содержимым.
    - save_vault_file не оставляет временных файлов после успешной записи.
    - Повторная запись (перезапись существующего хранилища) не приводит
      к потере данных при "падении" процесса между записью во временный
      файл и os.replace() (эмулируем через мок/патч, где это возможно).
"""
