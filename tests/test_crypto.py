"""
Тесты для vault.crypto.

ЗАГЛУШКА. Будут реализованы вместе с vault/crypto.py.

Планируемые тест-кейсы:
    - encrypt_vault -> decrypt_vault возвращает исходные данные без изменений
      (round-trip).
    - decrypt_vault с неверным мастер-паролем должна корректно завершаться
      ошибкой (а не тихо возвращать мусор) — проверяем, что AES-GCM
      действительно детектирует подмену/неверный ключ через auth tag,
      и что проверка canary-строки отлавливает оставшиеся случаи.
    - decrypt_vault с повреждённым (изменённым хотя бы на 1 байт)
      ciphertext или тегом аутентификации — тоже должна отклоняться.
    - Для одинаковых данных и одного мастер-пароля два вызова encrypt_vault
      дают РАЗНЫЕ salt/nonce/ciphertext (проверка, что соль и nonce
      генерируются заново каждый раз, а не переиспользуются).
"""
