"""
vault.storage — чтение и запись файла хранилища на диске.

ЗАГЛУШКА. Модуль будет реализован после vault/crypto.py.

Здесь появятся функции:
    load_vault_file(path) -> bytes
        Чтение сырых байт файла хранилища (без расшифровки).

    save_vault_file(path, blob: bytes) -> None
        Атомарная запись байт хранилища на диск: сначала во временный
        файл в той же директории, затем os.replace() поверх целевого
        файла. Это защищает от повреждения хранилища при сбое
        (отключение питания, ошибка на середине записи и т.п.) —
        os.replace() на POSIX и Windows является атомарной операцией.

Почему именно так — см. раздел "Атомарная запись" в CLAUDE.md.
"""
